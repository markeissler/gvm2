#!/usr/bin/env bash
#
# shellcheck shell=bash
# vi: set ft=bash
#
function gvm_implode() {
  read -p "Are you sure? [Y/n] " -n 1 -r
  echo
  if [[ ! $REPLY =~ ^[Yy]$ ]]; then
   __gvm_display_message "Action cancelled"
 else
   (rm -rf "$GVM_ROOT" && __gvm_display_message "GVM successfully removed") || __gvm_display_error "Failed to uninstall gvm"
 fi
}
