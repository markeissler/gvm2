Version is not installed but can be built from upstream sources or
downloaded as a pre-built binary from upstream sources:

- Build source -
    prompt> gvm install <version>

- Install binary -
    prompt> gvm install <version> --binary
